const API = '/api'

export const PATHS = {
  testing: `${API}/testing`,
  blogs: `${API}/blogs`,
  posts: `${API}/posts`,
  comments: `${API}/comments`,
  users: `${API}/users`,
  auth: `${API}/auth`,
  security: `${API}/security`,
} as const
